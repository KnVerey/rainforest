<h1>All products</h1>
<%= link_to button_tag("New product"), new_product_path %>

<% @products.each do |p| %>
	<h3><%= link_to "#{p.name}", product_path(p) %>
	<%= p.price_in_cents %></h3>
	<p><%= p.description %></p>
<% end %>