<% if @message.errors.any? %>
	<div class="error-explanation">
		<h4>We couldn't send your message because: </h4>
		<ul>
			<% @message.errors.full_messages.each do |msg| %>
				<li><%= msg %></li>
			<% end %>
		</ul>
	</div>
<% end %>